# ToramCalculator 需求、任务与 AUI

## 1. 文档范围

本文从用户需求推导核心任务，再以层级状态转换描述 ToramCalculator 的抽象用户界面（AUI）。

本文使用中文自然语言表达状态、父子关系和语义转换，不规定页面、布局、组件、控件、视觉样式或实现方式。

## 2. 用户需求

### 2.1 查询游戏事实

用户需要查找、核对和理解怪物、技能、装备、地点、任务等游戏事实，并沿资料关系继续探索。

### 2.2 建立机体

用户需要创建和维护机体，配置能力、装备、技能及其他战斗条件，并了解当前配置产生的属性和技能表现。

### 2.3 构造战斗假设

用户需要围绕机体、队伍、目标、成员流程和环境条件构造可运行的模拟设计，并在同一 Simulator 会话中保留多个 DesignCopy 作为不同假设。

### 2.4 执行实战验证

用户需要通过手动操作或成员流程执行模拟，观察战斗过程，并形成与 DesignCopy 对应的运行记录。

### 2.5 分析和比较结果

用户需要检查运行摘要、技能过程、输入轨迹和成员状态，并比较不同 DesignCopy 或不同实操过程形成的结果差异。

### 2.6 复用验证结果

用户需要根据分析结果继续修改 DesignCopy、将行动录制转为成员流程，或将选定 DesignCopy 回写为正式设计。

### 2.7 维护游戏知识

资料贡献者需要新增、修订和解释 Wiki 中的游戏事实，使查询、机体配置和模拟使用同一份知识。

### 2.8 访问分享资产

用户需要通过稳定地址打开同一 Character 或 Simulator。资产根据当前访问者身份呈现为只读或可编辑。

### 2.9 从推荐开始验证

用户需要根据目标和约束获得候选机体或模拟设计，并将候选作为 DesignCopy 进入正常验证过程。

## 3. 核心任务

### 3.1 主任务：验证一个战斗假设

```text
验证一个战斗假设
├── 查询相关游戏事实
│   ├── 搜索资料
│   ├── 浏览资料分类
│   ├── 查看资料详情
│   └── 查看关联资料
├── 准备机体
│   ├── 选择已有 Character
│   ├── 创建 Character
│   ├── 调整机体配置
│   ├── 查看派生属性
│   └── 查看技能表现
├── 构造模拟设计
│   ├── 选择或创建 Simulator
│   ├── 选择 DesignCopy
│   ├── 配置场景与阵容
│   ├── 配置成员
│   ├── 配置 DesignCopy 中的机体
│   ├── 配置成员流程
│   └── 配置分析范围
├── 执行验证
│   ├── 选择主控成员
│   ├── 控制成员行动
│   ├── 释放技能
│   ├── 控制验证进程
│   ├── 观察战斗状态
│   └── 结束验证并形成运行记录
├── 分析结果
│   ├── 查看运行摘要
│   ├── 查看技能过程
│   ├── 查看输入轨迹
│   ├── 检查成员状态
│   └── 比较运行记录
└── 继续设计
    ├── 返回当前 DesignCopy
    ├── 形成新的 DesignCopy
    ├── 将行动录制转为成员流程
    └── 回写选定 DesignCopy
```

### 3.2 补充任务：维护 Wiki

```text
维护 Wiki
├── 选择资料对象
├── 新增游戏事实
├── 修订游戏事实
├── 维护资料关系
└── 编写解释内容
```

### 3.3 补充任务：访问分享资产

```text
访问分享资产
├── 打开稳定地址
├── 查看 Character 或 Simulator
└── 在可编辑时继续修改
```

### 3.4 补充任务：从推荐开始验证

```text
从推荐开始验证
├── 指定目标与约束
├── 查看候选设计及依据
├── 选择候选设计
└── 将候选作为 DesignCopy 进入验证循环
```

## 4. App AUI

App AUI 由并行区域共同描述。当前工作区表示用户正在进行的活动；Character 会话和 Simulator 会话分别保持其持续业务状态。

```text
App AUI
├── 当前工作区
│   ├── 首页
│   ├── Wiki
│   ├── Character
│   ├── Simulator
│   └── 设置
├── Character 会话
│   ├── 未加载
│   └── 已加载
└── Simulator 会话
    ├── 未加载
    └── 已加载
        ├── 设计
        ├── 验证
        └── 分析
```

切换当前工作区不直接结束 Character 会话或 Simulator 会话。会话状态与当前工作区共同决定用户恢复任务时所处的位置。

## 5. 当前工作区 AUI

### 5.1 首页

```text
首页
├── 进入 Wiki -> 当前工作区.Wiki
├── 进入 Character -> 当前工作区.Character
├── 进入 Simulator -> 当前工作区.Simulator
└── 进入设置 -> 当前工作区.设置
```

首页只负责选择任务入口，不拥有 Wiki、Character 或 Simulator 的业务状态。

### 5.2 Wiki

```text
Wiki
├── 浏览
│   ├── 选择资料分类 -> 浏览
│   ├── 搜索资料 -> 查看搜索结果
│   ├── 选择资料对象 -> 查看资料
│   └── 开始新增资料 -> 维护.新增
├── 查看搜索结果
│   ├── 调整查询 -> 查看搜索结果
│   ├── 选择资料对象 -> 查看资料
│   └── 返回浏览 -> 浏览
├── 查看资料
│   ├── 查看关联资料 -> 查看资料
│   ├── 返回资料集合 -> 浏览
│   └── 开始修订资料 -> 维护.修订
└── 维护
    ├── 新增
    │   ├── 提交资料 -> 查看资料
    │   └── 放弃新增 -> 浏览
    ├── 修订
    │   ├── 提交修订 -> 查看资料
    │   └── 放弃修订 -> 查看资料
    └── 编辑解释内容
        ├── 提交内容 -> 查看资料
        └── 放弃编辑 -> 查看资料
```

资料提交后，用户回到对应资料的查看状态，并感知提交结果。

### 5.3 Character

```text
Character
├── 选择机体
│   ├── 选择已有 Character -> 配置
│   └── 创建 Character -> 配置
├── 配置
│   ├── 选择其他 Character -> 配置
│   ├── 修改基础配置 -> 配置，派生结果随当前配置更新
│   ├── 修改能力配置 -> 配置，派生结果随当前配置更新
│   ├── 修改装备配置 -> 配置，派生结果随当前配置更新
│   ├── 修改技能配置 -> 配置，技能表现随当前配置更新
│   ├── 修改其他机体配置 -> 配置，相关结果随当前配置更新
│   ├── 查看派生属性 -> 查看属性
│   └── 查看技能表现 -> 查看技能
├── 查看属性
│   ├── 检查属性及来源 -> 查看属性
│   └── 返回配置 -> 配置
└── 查看技能
    ├── 选择技能 -> 查看技能
    ├── 检查技能表现 -> 查看技能
    └── 返回配置 -> 配置
```

Character 中的配置修改成为当前机体配置；派生属性和技能表现依据当前机体配置更新。

### 5.4 设置

```text
设置
├── 浏览设置
│   ├── 选择设置类别 -> 编辑设置
│   └── 返回上一个工作区 -> 当前工作区.先前状态
└── 编辑设置
    ├── 修改设置 -> 编辑设置，相关应用行为随之更新
    └── 返回设置目录 -> 浏览设置
```

## 6. Character 会话 AUI

```text
Character 会话
├── 未加载
│   └── 打开 Character -> 已加载
└── 已加载
    ├── 当前 Character
    │   ├── 修改当前配置 -> 当前 Character，计算结果随之更新
    │   └── 请求切换 Character -> 切换中
    ├── 切换中
    │   ├── 切换完成 -> 当前 Character
    │   └── 切换失败 -> 当前 Character
    └── 结束会话 -> 未加载
```

当前工作区离开 Character 时，Character 会话可以继续保持已加载状态。

## 7. Simulator AUI

当前工作区进入 Simulator 时，呈现 Simulator 会话的当前业务状态。

```text
Simulator
├── 选择模拟方案
│   ├── 选择已有 Simulator -> Simulator 会话.已加载.设计
│   └── 创建 Simulator -> Simulator 会话.已加载.设计
└── 当前会话
    ├── 呈现设计状态 -> 设计
    ├── 呈现验证状态 -> 验证
    └── 呈现分析状态 -> 分析
```

### 7.1 Simulator 会话

```text
Simulator 会话
├── 未加载
│   └── 打开 Simulator -> 已加载.设计
└── 已加载
    ├── 设计
    ├── 验证
    ├── 分析
    ├── 切换模拟方案 -> 切换中
    └── 结束会话 -> 未加载
```

### 7.2 设计

```text
设计
├── 选择 DesignCopy
│   └── 选择副本 -> 编辑 DesignCopy
├── 编辑 DesignCopy
│   ├── 修改场景 -> 编辑 DesignCopy
│   ├── 修改阵容 -> 编辑 DesignCopy
│   ├── 修改成员 -> 编辑 DesignCopy
│   ├── 修改机体配置 -> 编辑 DesignCopy，派生结果随之更新
│   ├── 修改成员流程 -> 编辑 DesignCopy
│   ├── 修改分析范围 -> 编辑 DesignCopy
│   ├── 查看与正式设计的差异 -> 检查差异
│   └── 开始验证 -> 验证.准备中
├── 检查差异
│   ├── 返回编辑 -> 编辑 DesignCopy
│   └── 回写 DesignCopy -> 编辑 DesignCopy，差异成为正式设计的一部分
└── 选择其他 DesignCopy -> 编辑 DesignCopy
```

### 7.3 验证

```text
验证
├── 准备中
│   ├── 准备完成 -> 运行中
│   └── 准备失败 -> 失败
├── 运行中
│   ├── 选择主控成员 -> 运行中
│   ├── 控制成员行动 -> 运行中，战斗状态随运行变化
│   ├── 释放技能 -> 运行中，输入结果进入运行过程
│   ├── 暂停验证 -> 已暂停
│   └── 结束验证 -> 结束中
├── 已暂停
│   ├── 继续验证 -> 运行中
│   ├── 单步推进 -> 已暂停，战斗状态推进
│   └── 结束验证 -> 结束中
├── 结束中
│   ├── 运行记录形成 -> 分析.查看运行
│   └── 结束失败 -> 失败
└── 失败
    ├── 重试验证 -> 准备中
    └── 返回设计 -> 设计.编辑 DesignCopy
```

### 7.4 分析

```text
分析
├── 查看运行
│   ├── 选择运行记录 -> 查看运行
│   ├── 查看运行摘要 -> 查看运行
│   ├── 查看技能过程 -> 检查过程
│   ├── 查看输入轨迹 -> 检查过程
│   ├── 检查成员状态 -> 检查过程
│   ├── 选择两条运行记录 -> 对比运行
│   ├── 将行动录制转为成员流程 -> 设计.编辑 DesignCopy
│   └── 返回设计 -> 设计.编辑 DesignCopy
├── 检查过程
│   ├── 选择时间位置 -> 检查过程
│   ├── 选择成员或技能 -> 检查过程
│   └── 返回运行摘要 -> 查看运行
└── 对比运行
    ├── 更换对比运行 -> 对比运行
    ├── 检查设计差异 -> 对比运行
    ├── 检查操作差异 -> 对比运行
    ├── 检查结果差异 -> 对比运行
    └── 返回单次运行 -> 查看运行
```

### 7.5 切换模拟方案

```text
切换中
├── 确认切换 -> 结束当前会话，加载目标 Simulator -> 已加载.设计
└── 取消切换 -> 返回当前业务状态
```

## 8. 推荐 AUI

推荐不是独立工作区，而是形成 DesignCopy 的任务入口。

```text
推荐
├── 指定目标与约束
├── 生成候选
│   ├── 生成完成 -> 查看候选
│   └── 生成失败 -> 指定目标与约束
└── 查看候选
    ├── 查看候选依据 -> 查看候选
    ├── 选择其他候选 -> 查看候选
    └── 采用候选 -> Simulator 会话.已加载.设计.编辑 DesignCopy
```

## 9. 分享访问任务

```text
分享访问
├── 打开 Character 地址 -> Character
└── 打开 Simulator 地址 -> Simulator
```

资产以只读或可编辑方式呈现；分享访问不创建新的资产或独立业务状态。

## 10. 核心转换总览

```text
首页
├── Wiki
├── Character
├── Simulator
└── 设置

Wiki
└── 查询或维护游戏知识

Character
└── 配置机体 -> 派生属性与技能表现更新

Simulator
└── 设计
    └── 验证
        └── 分析
            ├── 返回设计
            ├── 形成成员流程
            └── 回写 DesignCopy

推荐
└── 候选设计 -> DesignCopy -> Simulator 验证循环
```
