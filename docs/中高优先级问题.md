## 5. virtualTable 重复实现列名解析和异常处理

文件：

components/ui/dataDisplay/virtualTable.tsx
371-381 <-> 418-429

两处都在做：

- 过滤隐藏列
- 从 dictionary 查找字段名称
- dictionary 缺失时 fallback 到列 ID
- 捕获异常并打印日志

区别只是：

- 一处渲染列可见性按钮
- 另一处渲染表头

结论：完整渲染逻辑不应合并，但列名解析确实重复。

建议：

getColumnLabel(columnId, dictionary)

统一处理：

- dictionary 字段存在
- 字典字段缺失
- fallback
- 日志策略

优先级：中高。

## 6. ExpressionTransformer 的两个转换入口重复整条 AST pipeline

文件：

engine/core/JSProcessor/ExpressionTransformer.ts
80-98 <-> 182-199
113-131 <-> 215-233

transform() 和 transformToGetValue() 都重复：

1. 解析 AST
2. 收集 member access
3. 标准化路径
4. 遍历访问节点
5. 检查 schema
6. 收集 dependencies
7. 记录 invalid paths
8. 应用 replacements
9. 处理成功和异常结果

差异只是 replacement 规则：

- transform 使用 valueProvider
- transformToGetValue 生成 getValue 或 getBaseValue

结论：真实核心重复。

建议：

- 抽取统一的 member-access transform pipeline。
- 通过 replacement callback 参数化替换策略。
- transformToGetValue 的 _ 基础值语义继续由该入口负责。
- 不要把 transform 和 transformToGetValue 直接合成一个面向所有调用者的宽泛 API。

优先级：高。

## 7. WorldState Reader 和 Writer 重复协议布局校验

文件：

engine/core/thread/worldStateBuffer.ts
751-773 <-> 1215-1237

WorldStateWriter 和 WorldStateReader 构造函数都校验：

- buffer byteLength
- memberCapacity
- areaCapacity
- attributeCount
- modifierSourceCapacity
- modifierChainCapacity
- WorldStateProtocolError

仓库已经有 readHeader()，但布局描述符匹配逻辑仍然复制。

结论：真实协议不变量重复，属于高风险边界。

建议：

- 抽取 validateBufferLayout(header, buffer, descriptor)。
- Writer 和 Reader 都调用同一纯函数。
- 保留两者各自的后续初始化逻辑。
- 错误码和异常类型必须保持一致。

优先级：高。

## 8. AttributeContainer 重复 modifier source 收集逻辑

  文件：

  engine/core/World/Member/runtime/AttributeContainer/AttributeContainer.ts
  841-853 <-> 931-943

  两处的 collect(type, attrIndex) 完全相同：

  - 从 modifierSources 按类型取值
  - 再按属性索引取值
  - 遍历 source entries
  - clone source
  - 返回 { source, value }[]

  另一个 clone：

  916-930 <-> 943-957

  还暴露出 modifier detail 类型结构重复。

  结论：真实重复，且属于同一类数据访问。

  建议：

  - 提取类私有方法，例如：
      - collectModifierSources(type, attrIndex)

  - 提取共享的 ModifierDetail 类型别名。
  - exportNestedValues() 和 exportModifierDetails() 仍然保留各自的输出结构。

  优先级：高。

  ## 9. 三套 agent class 编译和成员注入逻辑重复

  文件：

  engine/core/World/Member/runtime/Behavior/AiBehaviorRuntime.ts
  50-75 <-> BtManager.ts:84-112
  93-105 <-> BtManager.ts:132-145
  93-105 <-> BtContextFactory.ts:144-157

  重复内容包括：

  - 使用 new Function 编译 agent class
  - 注入 BehaviourTree
  - 注入 State
  - 注入 ModifierType
  - 创建 agent 实例
  - 读取实例自有属性
  - 读取 prototype 属性
  - 使用 Object.defineProperty 注入执行上下文
  - 遇到成员冲突时跳过

  这里不仅是代码重复，还存在行为差异：

  - BtContextFactory 会收集 warning
  - BtManager 使用 logger
  - AiBehaviorRuntime 失败时直接 return
  - 冲突日志格式不同
  - agent 初始化失败的可观测性不同

  结论：最重要的架构重复之一。

  建议：

  - 抽取底层的 agent compiler/member injector。
  - 通过回调参数化：
      - warning
      - logger
      - owner context

  - AiBehaviorRuntime、BtManager、BtContextFactory 保留各自的生命周期和上下文隔离。
  - 不要把三个运行时类强行合并。
  - 必须保留 BtManager 的 checkpoint/runtime 隔离语义。

  优先级：最高。

  ## 10. MobStateMachine 重复声明同名 interface

  文件：

  engine/core/World/Member/types/Mob/MobStateMachine.ts
  50-72 <-> 71-93

  以下 interface 重复了两次：

  - 收到快照请求
  - 收到目标快照
  - 进行伤害计算
  - 进行命中判定
  - 进行控制判定
  - 收到buff增删事件

  TypeScript 同名 interface 会进行 declaration merging，因此不一定立刻报错，但这是明显的遗留重复。

  结论：明确错误结构，低风险修复。

  建议：

  - 删除重复的一组声明。
  - 保留一组作为 MobSpecificEvent 的来源。
  - 删除后检查事件 union 是否仍包含所有事件。

  优先级：高，且改动风险低。

  ## 11. performance.ts 的装饰器和函数包装器重复完整测量流程

  文件：

  lib/utils/performance.ts
  421-452 <-> 471-499

  performanceMonitor() 和 monitorPerformance() 都重复：

  - 生成 event ID
  - performance.now()
  - 执行被包装函数
  - 计算 duration
  - 最小耗时过滤
  - 成功事件上报
  - 失败事件上报
  - 错误 metadata
  - 重新抛出错误

  结论：真实核心工具重复。

  建议：

  - 抽取统一的 measurePerformance()。
  - 类方法装饰器和函数包装器只负责适配调用形式。
  - 成功和失败的事件格式继续统一。
  - 注意保留方法 this 绑定。

  优先级：高。

   ## 12. CommonActions 重复技能等级和表达式求值流程

  文件：

  engine/core/World/Member/runtime/Agent/CommonActions.ts
  479-493 <-> 540-554
  483-493 <-> 645-655
  640-654 <-> 698-711

  涉及：

  - healHp
  - healMp
  - modifyAttribute
  - setAttributeModifier

  重复内容包括：

  - 从 context.skill 读取技能
  - fallback 到 currentSkill
  - 计算 skillLv
  - 调用 expression evaluator
  - 构造时间、tick、caster、target、skillLv 上下文
  - 判断是否返回数字
  - 记录 warning

  但这些 action 的业务语义不同：

  - heal 是回复并封顶
  - modify 是累加 modifier
  - set 是按 sourceId 覆盖
  - 错误日志和有限数检查也不同

  结论：公共“表达式求值上下文”是真实重复，modifier 语义不能合并。

  建议：

  - 抽取 resolveSkillLevel(context, explicitSkillLv?)。
  - 抽取 evaluateActionNumber(context, capabilities, expression)。
  - 由调用方决定错误消息和有限值策略。
  - 不要把 modifyAttribute 和 setAttributeModifier 合并成同一个 action。

  优先级：高。

  同文件另有一组可单独处理的几何重复：

  279-293 <-> 307-321

  moveAttack 和 groundLineAttack 的移动圆形范围、caster-to-target trajectory 完全相同，但 schema 和 rangeKind 不同。

  建议只抽取：

  resolveMovingCircleRange(input)

  优先级：中低。

  ## 13. 登录和注册接口重复 JWT 生成与 Cookie 设置

  文件：

  routes/api/auth/login.ts
  46-70 <-> routes/api/auth/register.ts:63-86

  两处都重复：

  - 构造 { sub, iat }
  - 读取 AUTH_SECRET
  - SignJWT
  - HS256
  - 30 天有效期
  - 设置 jwt Cookie
  - httpOnly
  - secure
  - sameSite
  - path
  - maxAge

  结论：真实重复，而且位于认证安全边界。

  建议：

  - 抽取统一的 createSessionToken(userId)。
  - 抽取统一的 setSessionCookie(token)。
  - 保留 login/register 各自的：
      - 输入校验
      - 用户查询或创建
      - HTTP status
      - 返回消息

  - 建议 helper 同时验证 AUTH_SECRET 是否存在，避免出现空 secret 的隐患。

  优先级：高。