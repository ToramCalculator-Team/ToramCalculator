# NNNN - <决策标题>

- **状态**: Proposed | Accepted | Deprecated | Superseded by NNNN
- **日期**: YYYY-MM-DD
- **决策层**: 数据层 / 计算层 / 编排层 / 通信 / 跨层
- **相关代码**: `path/to/file.ts:行号`（可选，多条）
- **相关 ADR**: NNNN、NNNN（可选）

> ADR 只记录决策、理由、边界和代价。阶段拆分、文件修改清单、具体命令、验收项和进度跟踪应放在任务、PR 或独立实施计划中。

## 背景

这个决策要解决什么问题？触发这个讨论的具体现象/需求是什么？

只保留理解决策所必需的事实和约束，不展开完整代码盘点。详细调查可以放在独立文档并在"参考"中链接。

## 候选方案

### A. <方案 A 简称>

方案描述（一两段，只写影响选择的关键差异）。

- 优点：…
- 缺点：…

### B. <方案 B 简称>

同上。

（可有更多方案。每个方案必须同时列优缺点；只列实际考虑过的主要方案，不复述完整讨论过程。）

## 决议

先用一句话说明选 X，以及这项决策确立的核心边界或契约。

理由（3-5 条要点）：

1. …
2. …

## 代价

这个决策放弃了什么？在什么情况下会让我们后悔？

**必填**。找不到代价意味着还没想清楚。

## 影响范围

只概括决策生效后受影响的职责和契约，不写执行步骤：

- 代码边界：哪些模块需要遵守这项决策
- 契约边界：哪些接口、数据语义或依赖方向发生变化
- 兼容约束：迁移期间必须保持什么；仅记录迁移策略，不记录具体操作
- 相关计划：任务、Issue、PR 或独立实施计划的链接（可选）

以下内容不属于本段：按阶段的迁移计划、逐文件修改清单、命令、验收清单、负责人和进度。

## 参考

- 原始讨论位置、相关设计文献、外部参考实现
