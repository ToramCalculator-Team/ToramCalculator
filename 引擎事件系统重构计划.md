# 引擎事件系统重构计划（7→3 通信机制收敛）

> 本文件是断点续作的工作清单。每次开工先读「当前进度」，做完一步就更新状态。
> 背景动机见 `src/lib/engine/document/decisions/0010-*.md` 与 `src/lib/engine/document/hook与触发层设计讨论结论.md`。

## 总目标

把引擎里七套都叫「事件」的机制按职责收敛为三个角色：
- **EventQueue** = 延迟 FSM 消息调度器（cross-frame scheduler）
- **ProcBus** = 成员内响应总线（passive/buff/registlet 在此声明「XXX 时 YYY」）
- **DomainEventBus** = 出 UI 投影（成员内事实 → 表现层）

分五步推进，风险递增。Step 1–3 已完成，Step 4–5 待做。

---

## 当前进度（截至最近一次会话）

- [x] **Step 1 术语正名**：EventQueue / ProcBus / DomainEventBus 三处类注释 + AGENTS.md 角色表。已 commit `520e92e`。
- [x] **Step 2 收窄 BT capabilities**：移除零调用的 runPipeline，订阅能力标 @internal。已 commit `520e92e`。
- [x] **ADR 0010**：合并 ProcBus + AttributeWatcher。已 Accepted；ADR 头部 + README 索引状态已同步。
- [x] **Step 3a**：新增 `AttributeThresholdSource` 适配器 + `attr.crossed` 内置事件，与旧 watcher 并行跑通 HP 紧急回复试点。已 commit `ce8052d`。
- [x] **Step 3b**：installer / watchThreshold / capabilities 全部切到 ProcBus，删除旧 `AttributeWatcherRegistry`。tsc 通过。已 commit `ce8052d`。
- [x] **Step 3 收尾**：BtManagerEnv 注释、适配器文件清理在 `ce8052d` 已落地；README 索引 ADR 0010 状态本轮同步为 Accepted。收尾清单原描述滞后于代码（详见下）。
- [ ] **Step 3.5 已观测问题验证与修复**（见下「Step 3.5」）：问题 A 已实测确认为正确性 bug，B/C 已静态坐实，D 表面。待修复。
- [ ] **Step 4 消除双总线**：DomainEventBus 降为下游投影。要 ADR。
- [ ] **Step 5 Pipeline emit 纯度**：管线 emit 改返回值。要 ADR，最后做。

---

## Step 3 收尾清单（开工先做这个，很快）

1. **BtManagerEnv.ts 类注释**（非阻塞，未改）：`src/lib/engine/core/World/Member/runtime/BehaviourTree/BtManagerEnv.ts` 约 40–46 行的「能力面收窄约定」注释里仍写着旧能力名 `watch / unwatchBySource`。应改为 `registerThreshold / unregisterThresholdBySource`，并补一句「阈值穿越统一走 ProcBus 的 attr.crossed 事件」。上次因 Edit 工具不稳没改成。
2. **AttributeThresholdSource.ts 收尾**：文件头注释第 13 行附近残留一个空注释行；文件末尾有多余空行（约 200–202 行）。顺手清掉。
3. **跑验证**（可选但建议）：tsc 已过。若想再确认运行时正确，可临时写脚本验证「installer 装一个 thresholdWatcher → 改 HP 跨阈值 → handler 触发」，跑完即删（参照 3a 用过的方式：真实 `MemberBaseNestedSchema` + `ModifierType.DYNAMIC_FIXED` 抬/降 hp.current）。
4. **更新 ADR 0010 状态**：`src/lib/engine/document/decisions/0010-*.md` 头部 `状态: Proposed` → `Accepted`（代码已落地）。README 索引同步状态列。
5. **提交**：Step 3a + 3b 一起 commit。建议信息：
   `refactor: AttributeWatcher 降格为 ProcBus 事件源（ADR 0010 落地）`
   说明要点：新增 AttributeThresholdSource 适配器 + attr.crossed 事件；installer/watchThreshold/capabilities 切到总线；删除旧 AttributeWatcherRegistry；成员内阈值响应统一走 ProcBus。

### Step 3 已改文件（已落盘，未提交）

```
M src/lib/engine/core/Event/BuiltInEvents.ts              （加 attr.crossed 事件 + schema）
A src/.../runtime/AttributeWatcher/AttributeThresholdSource.ts  （新适配器）
M src/.../World/Member/Member.ts                          （字段换 thresholdSource；setEventCatalog 接 emitter；capabilities 换 registerThreshold）
M src/.../attachments/RuntimeAttachmentInstaller.ts        （installThresholdWatchers 改 register+subscribe；uninstall 改 unregisterBySource）
M src/.../runtime/Agent/CommonActions.ts                   （watchThreshold/unsubscribeBySource 改走 ProcBus）
M src/.../runtime/BehaviourTree/BtManagerEnv.ts            （capabilities 接口 watch→registerThreshold；import 换源）
M src/components/features/BtEditor/model/previewRuntime.ts （mock 换 registerThreshold 空实现）
M src/.../runtime/StatContainer/StatContainer.ts           （注释旧名→新名）
M src/.../runtime/StatContainer/StatContainerTypes.ts      （注释旧名→新名）
D src/.../runtime/AttributeWatcher/AttributeWatcher.ts      （删除旧注册表）
```

### Step 3 关键设计点（实现时已确认，供回顾）

- **等价性**：旧 watcher 每注册一个 (path,threshold) 配一个 handler；新模型每个 register 仍独立 emit 一次 `attr.crossed`，订阅者按 sourceId 一一对应订阅、predicate 按 `path + threshold` 过滤。N 个注册 → N 次 emit → 各订阅者各收各的，不丢不错配。
- **冷却**：installer 的 `lastFiredTimeMs` 冷却保留在订阅闭包里（本就不进 checkpoint，靠重放重建）。
- **checkpoint**：Member.captureCheckpoint 本来就没有 attributeWatchers 段（只 checkpoint statContainer/statusStore/btManager），所以删旧 watcher 不涉及 checkpoint 改动。AttributeThresholdSource 自带 captureCheckpoint 仅调试用，靠安装逻辑重放恢复。
- **EventCatalog**：维持 worker 启动一次性冻结现状，`attr.crossed` 只占一个内置位（起步方案，不拆 per-path）。

---

## Step 3.5 — 已观测问题验证与修复（审阅期发现）

> 这是对 ADR 0010 落地代码的一轮独立审阅产物。问题 A 已实测确认；B/C/D 待逐条验证。
> 约定：每条先「验证」（写一次性脚本或静态确认），确认成立再「修复」。验证脚本跑完即删。

### 问题 A — 同 (path, threshold) 多源订阅重复 / 错向触发【已验证 ❌ 成立，正确性 bug】

- **现象**：两个不同 `sourceId` 注册相同 `(path="hp.current", threshold=25, "falling")`，HP 跨阈值**一次**，每个 handler 被调用 **2 次**（期望各 1 次）。N 个同点订阅 → N² 次 handler 调用。
- **验证方式（已执行）**：真实 `StatContainer`(hp.max=100/current=100) + `AttributeThresholdSource`→`ProcBus`，两个 sourceId 用 `watchThreshold` 同款 predicate 订阅 `attr.crossed`；`addModifier("hp.current", DYNAMIC_FIXED, -80)` + `flushDirtyValues()` 跨阈值。实测 `handlerA=2 handlerB=2`。
- **根因**：
  1. 适配器每条 `register` 各挂一条 `onChange`（`AttributeThresholdSource.ts:92`），跨越时**每条独立 emit 一次** `attr.crossed`。
  2. `ProcBus.emit` 是全广播（`ProcBus.ts:134`）。
  3. `watchThreshold` 的 predicate 只比 `path + threshold`（`CommonActions.ts:723-724`），**不比 direction、不认产生事件的那条注册**。
  4. → M 次 emit × N 个命中 predicate = 每 handler M×N 次。方向也未复查：`rising` 订阅者会被另一源的 `falling` 跨越错误唤醒。
- **与 ADR 的偏差**：ADR「代价」第 2 条把单 bit 后果描述为「唤醒全部订阅者再逐个比 path」的**性能扇出**；实测表明它同时是**正确性**漏洞（重复 + 错向触发）。等价性声明（本文件原 Step3 关键设计点「N 个注册 → 各收各的，不丢不错配」）**不成立**。
- **候选修复（择一，留待全部完成后定）**：
  1. `attr.crossed` payload 已带 `direction`；predicate 增加 `direction` 匹配 → 解决错向，但同 `(path,threshold,direction)` 多源仍重复。
  2. 让 emit 携带产生它的注册身份（如 `registrationId` 或 `sourceId`），订阅者 predicate 只认自己那条 → 根治重复 + 错向。倾向此项。
  3. 或：适配器对同 `(path,threshold,direction)` 去重，只挂一条 `onChange`、emit 一次；订阅端按 path+threshold+direction 过滤。需重新界定「N 注册 vs N emit」语义。
- **同步要改的文档**：ADR 0010「代价」节补「正确性」维度；本文件 Step3 关键设计点「等价性」条订正。

### 问题 B — `ThresholdEntry.lastValue` 死字段【待静态确认】

- **疑点**：`lastValue` 在 register 采样（`AttributeThresholdSource.ts:89`）、handleChange 更新（:144），但 `detectCrossing` 用 onChange 传入的 `oldValue/newValue`、`tryFireAtRegister` 用 `initialValue` 参数——`lastValue` 疑似**只写不读**。
- **验证方式**：grep `lastValue` 全部引用，确认无读取点。
- **修复**：确认后删字段（旧 watcher 残留状态）。

### 问题 C — `AttributeThresholdSource.captureCheckpoint` 孤儿 API【待确认接线】

- **疑点**：适配器有 `captureCheckpoint`（:129-138），但 Member.captureCheckpoint 不含 attributeWatchers 段（本文件 Step3 关键设计点已述）。疑似未接线的孤儿方法，易误导「阈值源有 checkpoint 保障」。
- **验证方式**：grep Member / MemberCheckpoint 是否调用 `attributeThresholdSource.captureCheckpoint`。
- **修复**：确认未接线后，删方法（或在注释明确标「仅调试、不进 checkpoint」）。

### 问题 D — 命名残留 `WatchDirection` / `@deprecated WatchOptions`【表面，最后清】

- **疑点**：ADR 想退役 "Watch" 词汇，但 `AttributeThresholdSource.ts:20,51-52` 仍保留 `WatchDirection`、`@deprecated WatchOptions`；`BtManagerEnv.ts:7` 仍 import 这俩类型名。
- **修复**：改名为 `ThresholdDirection` / `RegisterOptions`，更新 import 点。纯表面，放最后。

---

## Step 4 — 消除双总线（中风险，要 ADR）

### 问题（已据代码现状校正）

调研发现计划原文的问题陈述与代码不符，校正如下：

- **现状不是"双发"，而是"对内那条根本没接"**：受击结算 `DamageResolution.resolveDamageAndApply` 走完**只** emit 了一次 `notifyDomainEvent({type:"hit"})`（出 UI），**从未** emit `damage.received` 到 ProcBus。全仓库搜不到任何 `emit("damage.received")`。
- **两个成员内事件源是空壳**：`damage.received` 与 `status.entered/exited` 都已在 `BuiltInEvents.ts` 登记、订阅侧（`installSubscriptions`）也写好了按 `damageTags`/`statusTypes` 过滤的逻辑，但**源头从没派发**。后果：数据驱动的 registlet（如"燃烧的斗志"=受着火伤害回 MP）现在**根本触发不了**——机制齐了，就差受击/状态端没接线。
- **叶子层散发**：`healHp`/`healMp` 等叶子直接调 `notifyDomainEvent`，是"对外投影"在叶子层的散乱出口。

### 目标方向（已与用户对齐，否定计划原方向）

**不做**计划原文的"DomainEventBus 降为 ProcBus 下游"。理由：ADR-0010 刚确立 ProcBus（对内响应）与 DomainEventBus（对外投影）**各司其职**的角色划分；把两者改成上下游会让对外投影耦合成员内事件结构（payload 不同、ProcBus 每成员而 DomainEventBus 引擎级单例），违背刚立的划分。

改为：**"成员身上发生的事实"在权威结算点一次性、成对地派发到两条总线**——对内 emit 给 ProcBus（供 passive/registlet 响应），对外 emit 给 DomainEventBus（供 UI 投影）。两次 emit 不是冗余，是两个不同消费方（消费方不同、payload 不同）。要消除的是"叶子各自乱调 `notifyDomainEvent` 而成员内响应源悬空"，不是"两条总线合一"。

附带交付：把 `damage.received` / `status.entered/exited` 两个空壳源接上，让"燃烧的斗志"等数据驱动 registlet 真正可跑，作为本步的运行时验证。

### 调研结论（已完成，代码坐实）

1. **`notifyDomainEvent` 调用点**：`DamageResolution.ts:146/227`（hit）、`CommonActions.ts:387/446`（healHp/healMp 发 state_changed）、`PlayerStateMachine.ts:202`（skill_cast_denied）、`MobStateMachine.ts:330`（转发 sender）。`Member.ts:542` → `domainEventSender`。
2. **DomainEventBus 是引擎级单例**（`GameEngine.ts:270`），`ControllerEventProjector` 订阅它按 binding 投影 memberId→controllerId。ProcBus 是**每成员**的。两者层级不同，印证不该改成上下游。
3. **`damage.received` 是空壳**：登记于 `BuiltInEvents.ts:100`，订阅逻辑在 `installSubscriptions`（`RuntimeAttachmentInstaller.ts:217`）就绪，但无任何 emit。`status.entered/exited` 同样空壳。
4. **受击结算 `resolveDamageAndApply` 是 Player/Mob 共用**（Mob `MobStateMachine.ts:312` 调用，Player 同），但 **registlet 仅 Player 安装**（`Player.ts:121` 才有 `installRuntimeAttachment`）。结论：emit `damage.received` 接在共用结算函数里，Mob emit 空转无害，无需类型判断——订阅侧不对称天然达成"registlet 是 Player 专属"的隔离。
5. **`ampr`（攻回）在 `PlayerAttrSchema.ts` 已存在**（1457 行等），"燃烧的斗志"可用真实公式 `ampr × tLv × 5%` 还原，无需补属性。emit 通路沿用 `resolveDamageAndApply` 现有的参数注入模式（新增一个 emit 参数，类比 `notifyDomainEvent`）。

### 落地步骤（ADR 0011 Accepted 后）

1. 先写 **ADR 0011**（Proposed），把上面方向与结论写死；状态走 README 索引。（任务 #8）
2. `resolveDamageAndApply` 命中分支末尾，在现有 `notifyDomainEvent(hit)` 旁新增 emit `damage.received` 到本成员 ProcBus（payload 按 `DamageReceivedPayloadSchema`：finalDamage/hit/crit/isFatal/sourceId/**damageTags**/frame）。emit 通路从 Member 经参数注入。（任务 #6）
3. `StatusInstanceStore` apply/移除路径接上 emit `status.entered/exited`（payload 按对应 schema）。（任务 #7）
4. 用真实 registlet 数据声明"燃烧的斗志"，端到端验证：Player 装该 registlet → 受带 `ignition` tag 的伤害 → 断言 mp.current 增加 `ampr×level×5%`。跑完即删脚本。（任务 #9）
5. 叶子层 `healHp/healMp` 的 `notifyDomainEvent` 散发问题**本步先不动**（属"对外投影出口收敛"，与本步"接通对内源"正交；留待后续，避免范围蔓延）。
6. tsc + 运行时验证全绿。

> 注：计划原文的"DomainEventBus 订阅 ProcBus / 删第二次 emit / 移除 capabilities.notifyDomainEvent"等步骤已**作废**，原因见上「目标方向」。

---

## Step 5 — Pipeline emit 纯度（高风险，要 ADR，最后做）

### 问题

计算层 Pipeline 通过 `emit` 算子（Member.ts 的 `pipelineEventSink`）在管线执行**中途**主动推编排层 ProcBus。这破坏了计算层的纯度和确定性回放：管线本应是「输入 → 输出」的纯计算，中途产生副作用（emit）会让重放结果依赖派发时序。

### 目标方向（已选）

移除管线中途 emit，改为**返回值**：管线执行收集 `emittedEvents[]` 作为输出的一部分，编排层在 `runPipeline` 返回后统一派发。计算层回到纯函数，派发时机由编排层掌控。

### 动手前要调研的点

1. `emit` 算子在所有 overlay / stage 的使用点：找 Pipeline instruction 里 `emit` 的定义和用例。
2. `pipelineEventSink` 当前被谁调用、emit 的事件去向（Member.ts setEventCatalog 里接的那个）。
3. 收集返回值的改造面：Pipeline 执行结果结构要加 `emittedEvents` 字段，所有 `runPipeline` 调用点要消费它。
4. 对预览模式确定性的影响：改返回值后，预览/回放是否更可控（这是收益，要在 ADR 里讲清）。

### 落地步骤（ADR Accepted 后）

1. 先写 ADR（任务 #6，Proposed）。
2. Pipeline 执行结果加 `emittedEvents: Array<{name,payload}>`；`emit` 算子改为往这个数组 push，不再调 sink。
3. 所有 `runPipeline` 调用点：拿到结果后把 `emittedEvents` 逐个 emit 到 ProcBus。
4. 移除 `pipelineEventSink`。
5. tsc + 运行时验证：管线触发的 proc（如命中后效果）仍正确派发，且回放确定。

---

## 工作方式备注

- **网络不稳时**：本会话中 Edit 工具多次因网络中断未落盘（界面显示成 `court` 字样）。遇到时改用 Bash + Python 做精确文本替换（`python3 - <<'PY' ... PY`，先 assert 匹配数==1 再写），比 Edit 可靠。单行替换可用 sed。
- **验证套路**：项目没有测试框架（package.json 无 test 脚本，引擎无 .test.ts）。验证用一次性 tsx 脚本：放在被测目录旁、相对 import、跑完 `rm` 删除。真实 schema 用 `MemberBaseNestedSchema`，改属性用 `statContainer.addModifier(path, ModifierType.DYNAMIC_FIXED, delta, source)` + `flushDirtyValues()`。
- **tsc 命令**：`npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "form.tsx" | grep -iE "error TS"`。`form.tsx` 有预存的无关错误，过滤掉。
- **ADR 编号**：当前最新是 0010。Step 4、Step 5 的 ADR 应为 0011、0012（注意：之前误建过一个 distributed-actor 的 0010 已删，不要复用编号）。
